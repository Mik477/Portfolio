// src/routes/debug/locale/+server.ts
// Debug locale server endpoint removed for static hosting; keeping file to avoid missing import errors.
// Exporting prerender true so adapter-static treats this as a static no-op.
export const prerender = true;
